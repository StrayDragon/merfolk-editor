# FR-10: 形状选择器优化

> 优先级: P1 | 状态: ⬜ 待开始

## 📋 概述

优化节点形状选择器，支持 FR-01 和 FR-03 引入的 30+ 新形状。

---

## 🎯 需求

### 10.1 形状面板

- 可视化形状预览
- 分类展示 (基础、流程、数据、特殊)
- 搜索/筛选
- 最近使用

### 10.2 形状分类

**基础形状:**
- rect, rounded, circle, diamond, hexagon

**流程控制:**
- stadium, subroutine, cylinder, doublecircle

**数据形状:**
- trapezoid, inv_trapezoid, lean_right, lean_left

**文档形状:**
- doc, card, waved-doc, lined-doc

**特殊形状:**
- bolt, hourglass, triangle, fork, delay

### 10.3 工具栏集成

- 工具栏按钮打开形状面板
- 或作为侧边栏常驻

### 10.4 拖拽创建

从形状面板拖拽到画布创建节点。

---

## 🔧 实现

### ShapePanel 组件

**新文件:** `src/components/ShapePanel.svelte`

```svelte
<script lang="ts">
  import { SHAPE_CATEGORIES } from '$core/constants';
  import { shapePreview } from '$canvas/shapes';

  export let onSelect: (shape: string) => void;
  export let onDragStart: (shape: string, e: DragEvent) => void;

  let filter = '';
  let expandedCategory: string | null = 'basic';

  $: filteredCategories = SHAPE_CATEGORIES.map(cat => ({
    ...cat,
    shapes: cat.shapes.filter(s =>
      s.name.toLowerCase().includes(filter.toLowerCase()) ||
      s.aliases?.some(a => a.toLowerCase().includes(filter.toLowerCase()))
    )
  })).filter(cat => cat.shapes.length > 0);
</script>

<div class="shape-panel">
  <input
    type="search"
    placeholder="搜索形状..."
    bind:value={filter}
  />

  {#each filteredCategories as category}
    <div class="category">
      <button
        class="category-header"
        on:click={() => expandedCategory = expandedCategory === category.id ? null : category.id}
      >
        <span>{category.name}</span>
        <span>{expandedCategory === category.id ? '▼' : '▶'}</span>
      </button>

      {#if expandedCategory === category.id}
        <div class="shapes-grid">
          {#each category.shapes as shape}
            <button
              class="shape-item"
              title={shape.name}
              on:click={() => onSelect(shape.id)}
              draggable="true"
              on:dragstart={(e) => onDragStart(shape.id, e)}
            >
              <svg width="40" height="30" viewBox="0 0 40 30">
                {@html shapePreview(shape.id, 40, 30)}
              </svg>
              <span class="shape-name">{shape.name}</span>
            </button>
          {/each}
        </div>
      {/if}
    </div>
  {/each}
</div>

<style>
  .shape-panel {
    width: 200px;
    height: 100%;
    overflow-y: auto;
    background: var(--bg-secondary);
    border-right: 1px solid var(--border-color);
    padding: 8px;
  }

  input[type="search"] {
    width: 100%;
    padding: 6px 10px;
    border: 1px solid var(--border-color);
    border-radius: 4px;
    margin-bottom: 8px;
  }

  .category-header {
    width: 100%;
    display: flex;
    justify-content: space-between;
    padding: 8px;
    background: transparent;
    border: none;
    cursor: pointer;
    font-weight: 500;
  }

  .shapes-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 4px;
    padding: 4px;
  }

  .shape-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 4px;
    border: 1px solid transparent;
    border-radius: 4px;
    background: transparent;
    cursor: grab;
  }

  .shape-item:hover {
    border-color: var(--primary-color);
    background: var(--bg-hover);
  }

  .shape-name {
    font-size: 0.7em;
    text-align: center;
    margin-top: 2px;
  }
</style>
```

### 形状常量

**文件:** `src/core/constants.ts` 增加

```typescript
export const SHAPE_CATEGORIES = [
  {
    id: 'basic',
    name: '基础',
    shapes: [
      { id: 'rect', name: '矩形', aliases: ['square', 'rectangle'] },
      { id: 'rounded', name: '圆角矩形', aliases: ['round'] },
      { id: 'circle', name: '圆形', aliases: ['circ'] },
      { id: 'diamond', name: '菱形', aliases: ['decision'] },
      { id: 'hexagon', name: '六边形', aliases: ['hex'] },
    ]
  },
  {
    id: 'flow',
    name: '流程',
    shapes: [
      { id: 'stadium', name: '体育场形', aliases: ['terminal', 'pill'] },
      { id: 'subroutine', name: '子程序', aliases: ['subprocess'] },
      { id: 'cylinder', name: '圆柱体', aliases: ['database', 'db'] },
      { id: 'doublecircle', name: '双圆', aliases: ['stop'] },
    ]
  },
  {
    id: 'data',
    name: '数据',
    shapes: [
      { id: 'trapezoid', name: '梯形' },
      { id: 'inv_trapezoid', name: '倒梯形' },
      { id: 'lean_right', name: '右倾', aliases: ['input'] },
      { id: 'lean_left', name: '左倾', aliases: ['output'] },
    ]
  },
  {
    id: 'document',
    name: '文档',
    shapes: [
      { id: 'doc', name: '文档', aliases: ['document'] },
      { id: 'card', name: '卡片', aliases: ['notched-rect'] },
      { id: 'odd', name: '旗帜', aliases: ['flag'] },
    ]
  },
  {
    id: 'special',
    name: '特殊',
    shapes: [
      { id: 'bolt', name: '闪电', aliases: ['lightning'] },
      { id: 'hourglass', name: '沙漏', aliases: ['collate'] },
      { id: 'triangle', name: '三角形', aliases: ['tri'] },
      { id: 'delay', name: '延迟' },
      { id: 'fork', name: '分叉' },
    ]
  },
];
```

### 拖拽处理

**文件:** `src/components/InteractiveCanvas.svelte`

```svelte
<script>
  function handleDrop(e: DragEvent) {
    const shapeId = e.dataTransfer?.getData('shape-id');
    if (!shapeId) return;

    const rect = canvasRef.getBoundingClientRect();
    const x = e.clientX - rect.left;
    const y = e.clientY - rect.top;

    // 创建节点
    history.execute(new AddNodeCommand(model, {
      id: generateNodeId(),
      text: 'New Node',
      shape: shapeId,
      x, y
    }));
  }
</script>

<div
  class="canvas"
  on:drop={handleDrop}
  on:dragover|preventDefault
>
  <!-- ... -->
</div>
```

---

## ✅ 验收标准

### 测试用例

```typescript
describe('ShapePanel', () => {
  it('搜索过滤形状', () => {
    render(ShapePanel, { onSelect: vi.fn(), onDragStart: vi.fn() });

    const input = screen.getByPlaceholderText('搜索形状...');
    fireEvent.input(input, { target: { value: 'circle' } });

    expect(screen.getByTitle('圆形')).toBeVisible();
    expect(screen.queryByTitle('矩形')).not.toBeVisible();
  });
});
```

### E2E 测试

```typescript
test('从形状面板拖拽创建节点', async ({ page }) => {
  const shape = page.locator('.shape-item[title="菱形"]');
  const canvas = page.locator('.canvas');

  await shape.dragTo(canvas, {
    targetPosition: { x: 200, y: 200 }
  });

  await expect(page.locator('.node[data-shape="diamond"]')).toBeVisible();
});
```

---

## 🔗 依赖

- 前置: FR-01 (新形状解析), FR-03 (新形状渲染)
- 被依赖: 无
